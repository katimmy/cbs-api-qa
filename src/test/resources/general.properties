# target execution: local or grid
target = local

# initial URL
url.base = "http://stage.api.finexio.com"

# global test timeout
timeout = 20

# javafaker locale
faker.locale = pt-BR

# headless mode only for chrome or firefox and local execution
headless = false
